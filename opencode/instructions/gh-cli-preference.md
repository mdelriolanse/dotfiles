# GitHub CLI Preference

Always use `gh` CLI for remote repository interactions — issues, PRs, repo listing, etc. — instead of the GitHub MCP tools or raw GitHub API calls.

For git operations (commit, push, clone, branch, etc.), continue using `git` CLI directly.
