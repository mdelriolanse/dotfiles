<!-- Native user-context slot only. Always-on text is rules/*.mdc (alwaysApply: true), injected into the system prompt. Do not paste those bodies here: omp drops an always-apply rule whose body already appears in a context file. -->
